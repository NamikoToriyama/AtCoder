#include <stdio.h>
#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int x, a;
    cin >> x >> a;
    if (x < a)
    {
        cout << 0 << endl;
    }
    else
    {
        cout << 10 << endl;
    }
}
